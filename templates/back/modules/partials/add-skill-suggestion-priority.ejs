<%
    let numberIndex = Number(index);
    let boolStarter = JSON.parse(starter);
%>

<input type="number" id="skill-priority-<%= index + "-" + indexPriority %>" class="form-control priority<%= !boolStarter ? " mt-2" : "" %><%= boolStarter ? " starter" : "" %>" <%= numberIndex === 0 ? 'value=1111 readonly=readonly' : '' %> />
