<!DOCTYPE html>
<html lang="<%= global.utils.locales.language %>">
    <%
        let data = (() => {
            let object = {};

            object.head = {
                title: "Modifier une cosmo",
                viewStyleUrls: [
                    "<link defer='defer' rel='stylesheet' href='../../css/back/views/update-cosmo.css' type='text/css'>"
                ],
                viewScriptUrls: [
                    "<script defer='defer' src='../../public/dist/back/js/update-cosmo.js' type='text/javascript'></script>"
                ]
            };
            object.dropdown = [
                {
                    url: "/back/update-cosmo/:" + cosmo._id,
                    icon: "create",
                    title: "Modifier une cosmo",
                    visibility: [
                        "production",
                        "development"
                    ]
                }
            ];
            object.activeIndex = "3.2";
            object.icon = "create";

            return object;
        })(),
        modalData = {
            title: "Warning!",
            id: "response-modal",
            titleId: "success-modal-id"
        };
    %>
    <%- include("./../layout/head", {head: data.head}) %>

    <body>
        <section class="wrapper">
            <%- include("./../layout/menu", {data: data}) %>

            <div id="content">
                <div class="container-fluid">
                    <%- include("./../modules/update-cosmo", {data: data}) %>
                </div>
            </div>
        </section>
        <%- include("./../modules/modal/response", {data: modalData}) %>
    </body>
</html>
